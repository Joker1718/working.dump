-- init.lua — Creation Forge: Dining Set add-on. Entry point (DF runs it at worldgen).
-- Requires the base "The Creation Forge" mod to be loaded first (see info.txt REQUIRES_ID).
require("cf_dining")
