-- init.lua — Creation Forge: Sandbox Tools add-on. Entry point (DF runs it at worldgen).
-- Requires the base "The Creation Forge" mod to be loaded first (see info.txt REQUIRES_ID).
require("cf_sandbox")
