-- init.lua — Creation Forge: Fantasy Metals add-on. Entry point (DF runs it at worldgen).
-- Requires the base "The Creation Forge" mod to be loaded first (see info.txt REQUIRES_ID).
-- To test, temporarily add a line `debug_level = 1` above the require (remove before publishing).
require("cf_metals")
