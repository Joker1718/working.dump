-- init.lua — Creation Forge: Trap & Ammo add-on. Entry point (DF runs it at worldgen).
-- Requires the base "The Creation Forge" mod to be loaded first (see info.txt REQUIRES_ID).
require("cf_trapammo")
